import React from "react";

const Jumbotron = () => (
  <div className="jumbotron text-center">
    <h1>Space Adventure Clicky Game!</h1> 
    <h2>Click on an image to earn points, but don't click on any more than once!</h2>
  </div>
);

export default Jumbotron;
